// const buyCard = document.querySelector('.buy-card');

// buyCard?.addEventListener('click', async (e) => {
//   e.preventDefault();
//   const card = e.target.closest('[data-id]');
//   const cardId = card.dataset.id;
//   const response = await fetch(`/orders/${cardId}`, {
//      method: 'P'
//     });
// });
