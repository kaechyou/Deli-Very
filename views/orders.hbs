<div class="cards">
  {{#each products}}
  <a href="/orders/details/{{id}}" class="no-dec">
    <div class="card">
      <div class="card-shadow"></div>
      <div class="main-img-box">
        <img class="main-img" src="{{img}}" class="card-img-top" alt="/images/logo.png">
      </div>
      <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <div class="price">
          <div class="start-price">{{price}} руб.</div>
          <div class="discount">-{{discount}}%</div>
          <div class="final-price">{{finalPrice}}</div>
        </div>
        <div class="card-btn">Адрес: {{location}}</div>
      </div>
    </div>
  </a>
  {{/each}}
</div>
